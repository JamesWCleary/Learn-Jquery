$(document).ready(function() {

	//BASIC SELECTORS
	$("body p.lead").css("border", "4px solid red") // CSS STYLE SELECT
	$("#id").css("border", "4px solid grey") // ID SELECT
	$("*").css("border", "4px solid blue") // ALL SELECT

	// BASIC ANIMATIONS
	$(".box:first").hide
	// INDEX FILTERS

	// RELATIONSHIP FILTERS

	// ATTRIBUTE FILTERS


});
